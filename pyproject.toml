[project]
name = "api-afeld-me"
version = "0.1.0"
description = "Python dependencies for tense checking"
dependencies = [
    # needed for installing spaCy model
    "pip",
    "pytest",
    "spacy",
]
